
### 텍스트 데이터 다루기

#### 단어 임베딩 이해하기

- 어떤 데이터를 벡터로 표현한 것을 **임베딩** 이라고 부른다.

- 단어(토큰)의 벡터는 단어 임베딩 이라 함

- 대표적인 단어 임베딩 모델은 Word2Vec


* 임베딩 벡터의 값으로 우린 이해할 수 없지만 유사한 텍스트들은 가까운 값에 위치해 있다.


#### 텍스트 토큰화하기


```
입력 테스트 : This is an example.
                      |
토큰화된 텍스트 : This  ,  is ,   an ,  example , .
                      |
토큰 ID : 40134,      2052,     133   ,   389   ,   12 
                      |
토큰 임베딩 : ㅁㅁㅁ ,  ㅁㅁㅁ ,   ㅁㅁㅁ,   ㅁㅁㅁ ,    ㅁㅁㅁ 

                      |

                  디코더 전용 트랜스포머 

                      |

                  후처리 단계

                      |

                  출력 텍스트

```


먼저 공백을 기준으로 나누어보기 

예시) 아래와 같은 예시로 공백을 기준으로 토큰화시키려고 함

```
import re

text="Hello, world. This is a test"
result = re.split(r'(\s)', text)

print(result)

```
<img width="715" height="32" alt="image" src="https://github.com/user-attachments/assets/6f309790-439c-4500-9468-e6d4aa1cca95" />



#### 토큰을 토큰 ID로 변환하기

- 입력 텍스트를 공백을 기준이나 어떠한 기준으로 토큰화를 시킨 후 이를 어휘사전에 번호를 붙여 등록(토큰 ID로 등록)

```

import re

# 간단한 문장들 불러오기
with open("the-verdict.txt", "r", encoding="utf-8") as f:
    raw_text = f.read()
    
    
preprocessed = re.split(r'([,.:;?_!"()\']|--|\s)', raw_text)
preprocessed = [item.strip() for item in preprocessed if item.strip()]
print(preprocessed[:30])

# 총 토큰 개수 확인 
print(len(preprocessed))
4960

# 고유한 토큰으로 구성된 어휘사전 만들기

all_words = sorted(set(preprocessed))
vocab_size= len(all_words)

print(vocab_size)
1130

# 토큰 ID 부여하여 단어장 만들기
vocab = {token:integer for integer,token in enumerate(all_words)}

# 단어장(토큰 , 토큰 ID) 확인하기 
for i, item in enumerate(vocab.items()):
    print(item)
    if i >= 50:
        break


```

<img width="202" height="307" alt="image" src="https://github.com/user-attachments/assets/efe8001e-59c8-4119-9c5e-4a7e8fe85bb8" />


```
import re

class SimpleTokenizerV1:
    def __init__(self, vocab):
        self.str_to_int = vocab
        self.int_to_str = {i:s for s,i in vocab.items()}

    # 텍스트를 토큰 ID로 바꾸기
    def encode(self, text):
        preprocessed = re.split(r'([,.:;?_!"()\']|--|\s)', text) # 'hello,. world'

        preprocessed = [
            item.strip() for item in preprocessed if item.strip()
        ]
        ids = [self.str_to_int[s] for s in preprocessed]
        return ids

    # 토큰 ID를 텍스트로 바꾸기
    def decode(self, ids):
        text = " ".join([self.int_to_str[i] for i in ids])
        # 구둣점 문자 앞의 공백을 삭제합니다.
        text = re.sub(r'\s+([,.?!"()\'])', r'\1', text)
        return text
    
    # 단어장을 만드는 문장들 불러오기
with open("the-verdict.txt", "r", encoding="utf-8") as f:
    raw_text = f.read()
    
    
preprocessed = re.split(r'([,.:;?_!"()\']|--|\s)', raw_text)
preprocessed = [item.strip() for item in preprocessed if item.strip()]
print(preprocessed[:30])


# 고유한 토큰으로 구성된 어휘사전 만들기

all_words = sorted(set(preprocessed))
vocab_size= len(all_words)

# 토큰 ID 부여하여 단어장 만들기
vocab = {token:integer for integer,token in enumerate(all_words)}

# 토크나이저를 사용해 텍스트를 정수로 인코딩(즉, 토큰화)
# 이 정수를 나중에 임베딩하여 LLM의 입력으로 사용할 수 있음    
tokenizer = SimpleTokenizerV1(vocab)

text = """"It's the last he painted, you know,"
           Mrs. Gisburn said with pardonable pride."""
ids = tokenizer.encode(text)
print(ids)

# 정수를 텍스트로 다시 디코딩
print(tokenizer.decode(ids))


```

#### 특수 문맥 토큰 추가하기

- 어휘사전(토크나이저)에 없는 단어를 추가해 어휘사전을 확장

- GPT-3 는 <|endoftext|>만 사용함 (<|unk|> 필요하지 않음)

- 다른 모델들은 여러개의 특수 문맥 토큰을 사용할 수 있음

- 독립적인 텍스트 소스를 모두 연결하여 사용하는데 긴 문장 텍스트들의 끝을 알려주는게 <|endoftext|>

```
import re

from SimpleTokenizerV2 import SimpleTokenizerV2


# 단어장을 만드는 문장들 불러오기
with open("the-verdict.txt", "r", encoding="utf-8") as f:
    raw_text = f.read()
    

preprocessed = re.split(r'([,.:;?_!"()\']|--|\s)', raw_text)
preprocessed = [item.strip() for item in preprocessed if item.strip()]
  


# 고유한 토큰으로 구성된 어휘사전 만들기

all_words = sorted(set(preprocessed))
vocab_size= len(all_words)

# 토큰 ID 부여하여 단어장 만들기
vocab = {token:integer for integer,token in enumerate(all_words)}

tokenizer = SimpleTokenizerV2(vocab)

# 알지 못하는 단어에 대해 인지하고 표현하기 위해서 <|unk|> 특수 토큰 추가
# 다른 문장을 이어 추가하여 학습시켜 어휘사전에 추가시키는 <|endoftext|> 특수 토큰 추가
all_tokens = sorted(list(set(preprocessed)))
all_tokens.extend(["<|endoftext|>", "<|unk|>"])

# 추가된 특수토큰 반영
vocab = {token:integer for integer,token in enumerate(all_tokens)}

# 추가된 어휘사전 갯수 확인 
print(len(vocab.items()))

# 특수토큰 마지막에 추가되었는지 확인
for i, item in enumerate(list(vocab.items())[-5:]):
    print(item)

tokenizer = SimpleTokenizerV2(vocab)

text1 = "Hello, do you like tea?"
text2 = "In the sunlit terraces of the palace."

text = " <|endoftext|> ".join((text1, text2))

print(text)

print(tokenizer.encode(text))

print(tokenizer.decode(tokenizer.encode(text)))


```

<img width="730" height="70" alt="image" src="https://github.com/user-attachments/assets/5baccc04-d584-47b0-976c-1b328ca1a4e6" />


#### 바이트 페어 인코딩

- GPT 계열에서 사용하는 토큰화 방법
  tiktoken 라이브러리로 구현되어 있음

- BPE 어휘사전 구축 방법
   개별 문자를 어휘사전에 등록

  자주 등장하는 문자 조합을 부분단어로 등록(최소 빈도 기준 2)
  ex) "d", "e"를 합쳐서 "de"를 등록

* 어휘사전에 없는 단어를 더 작은 부분 단어나 개별 문자로 분할하여 처리할 수 있다. 즉, 알지 못하는 단어를 지속적으로 추가하며 학습
